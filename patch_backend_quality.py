"""
Deprecated patch helper.

This file used to apply one-off historical patches for older SparseFlow
revisions. The fixes are already integrated in the current codebase.

It is intentionally kept as a no-op placeholder for backward compatibility.
"""

if __name__ == "__main__":
    print(
        "[SparseFlow] patch_backend_quality.py is deprecated and no longer applies "
        "runtime patches. Current repository already contains integrated fixes."
    )

